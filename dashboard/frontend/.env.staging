# Dashboard Frontend - Staging Environment
# Used when building with: npm run build:staging

# API Configuration
# Staging uses same-origin API calls (determined by window.location.origin)
# Dashboard served from same origin as Flask backend at http://65.181.112.77

# Development host override (not used in staging)
# VITE_DEV_HOST is only for local development
