# Dashboard Frontend Environment Configuration
# Copy to .env for local development overrides

# =============================================================================
# Development Configuration
# =============================================================================

# Override the development host (defaults to window.location.hostname)
# Useful when testing from different machines on local network
# VITE_DEV_HOST=192.168.1.100

# =============================================================================
# Notes
# =============================================================================

# The dashboard uses same-origin API calls (window.location.origin) for:
# - /api/* routes to Flask backend
# - /socket.io/* for WebSocket connections
#
# Service URLs are determined by:
# - Local network: Direct port access (e.g., http://localhost:8188)
# - External: Nginx reverse proxy (e.g., /comfyui/)
#
# Build modes:
# - npm run build              # Default build
# - npm run build:production   # Production optimized (--mode production)
# - npm run build:staging      # Staging build (--mode staging)
