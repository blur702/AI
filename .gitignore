# Large AI project clones (these have their own git repos)
alltalk_tts/
audiocraft/
ComfyUI/
DiffRhythm/
local-talking-llm/
MusicGPT/
stable-audio-tools/
stability-matrix/
Wan2GP/
YuE/

# Dependencies and models
node_modules/
ollama-models/

# Python
__pycache__/
*.pyc
*.pyo
.venv/
venv/
*.egg-info/

# Test artifacts
test-results/
*.log

# Runtime queue files
logs/ingestion_queue.txt

# Scraper runtime data (checkpoints, job state)
data/scraper/checkpoints/*.json
data/scraper/jobs.json
data/scraper/*_stdout.log
data/scraper/*_stderr.log

# Congressional scraper temp files
data/scraper/congressional/checkpoints/
data/scraper/congressional/heartbeats/
data/scraper/congressional/remaining_members.json
data/scraper/congressional/supervisor.pid
data/scraper/congressional/work_assignments.json
# All scraper cache directories (can be large)
data/scraper_cache/

# VSCode workspace settings (machine-specific)
.vscode/settings.json

# Claude Code local settings (machine-specific)
.claude/settings.local.json

# SSL certificates and private keys (never commit private keys!)
nginx/ssl/*.key
nginx/ssl/*.pem

# Weaviate data backups (too large for git)
weaviate_share/data/*.tar.gz
weaviate_share/data/*.tar

# Environment files (NEVER commit secrets!)
.env
.env.local
.env.*.local
**/.env
**/.env.local
!**/.env.example

# Allow staging/production env files ONLY if they contain no secrets
# (comment out these lines if your staging/production files have secrets)
# !**/.env.staging
# !**/.env.production

# Misc
nul
.DS_Store
Thumbs.db
.secrets/

# Media documentation (local reference files)
media/docs/

# MCP configuration (contains SSH credentials)
.mcp.json
