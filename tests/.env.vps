# VPS Test Environment Configuration Template
#
# IMPORTANT: This is a template file with placeholder values.
# DO NOT commit real hostnames, domains, or credentials to version control.
#
# Usage:
#   1. Copy this file to .env.local or .env
#   2. Replace placeholder values with your actual VPS configuration
#   3. Never commit the .env.local or .env file
#
# Example:
#   cp .env.vps .env.local
#   # Edit .env.local with your real values

# Environment identifier
TEST_ENVIRONMENT=vps

# Base URL for the dashboard (HTTPS via nginx)
# Replace with your VPS hostname
BASE_URL=https://your-vps-hostname.example.com
DASHBOARD_API_URL=https://your-vps-hostname.example.com

# API Gateway URL
GATEWAY_API_URL=https://your-vps-hostname.example.com:1301

# Individual service URLs (direct port access)
# Replace hostname with your VPS hostname
OLLAMA_URL=https://your-vps-hostname.example.com:11434
COMFYUI_URL=https://your-vps-hostname.example.com:8188
OPEN_WEBUI_URL=https://your-vps-hostname.example.com:3000
WEAVIATE_URL=https://your-vps-hostname.example.com:8080
ALLTALK_URL=https://your-vps-hostname.example.com:7851
WAN2GP_URL=https://your-vps-hostname.example.com:7860
YUE_URL=https://your-vps-hostname.example.com:7870
DIFFRHYTHM_URL=https://your-vps-hostname.example.com:7871
MUSICGEN_URL=https://your-vps-hostname.example.com:7872
STABLE_AUDIO_URL=https://your-vps-hostname.example.com:7873
N8N_URL=https://your-vps-hostname.example.com:5678

# Dashboard authentication (if enabled)
# DASHBOARD_AUTH_USERNAME=admin
# DASHBOARD_AUTH_PASSWORD=your-password

# Test configuration
TEST_TIMEOUT=120000
HEADLESS=true

# SSL/TLS configuration
# Set to 'true' if using self-signed certificates
# Production VPS with Let's Encrypt should use 'false'
ALLOW_INSECURE_CONNECTIONS=false

# Embedding models to preserve (comma-separated)
# These models will NOT be unloaded during VRAM cleanup
PRESERVE_EMBEDDING_MODELS=nomic-embed-text,mxbai-embed-large

# GPU-intensive services (comma-separated)
# These services consume significant VRAM and should be managed carefully
GPU_INTENSIVE_SERVICES=comfyui,stable_audio,wan2gp,yue,diffrhythm,audiocraft

# Service orchestration settings
SERVICE_START_TIMEOUT=60000
SERVICE_HEALTH_INTERVAL=5000
MAX_SERVICE_RETRIES=3
