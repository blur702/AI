{#
/**
 * @file
 * Atomic React theme implementation to display a node.
 *
 * Available variables:
 * - node: Full node entity.
 * - label: The title of the node.
 * - author_name: Node author full name.
 * - metadata: Metadata for this node.
 * - date: Themed creation date field.
 * - url: Direct URL of the current node.
 * - display_submitted: Whether submission information should be displayed.
 * - content: All node items.
 * - author_picture: The node author user entity.
 * - attributes: HTML attributes for the containing element.
 * - title_attributes: Same as attributes except applied to the main title tag.
 * - content_attributes: Same as attributes except applied to content region.
 * - author_attributes: Same as attributes except applied to author information.
 * - title_prefix: Additional output populated by modules.
 * - title_suffix: Additional output populated by modules.
 * - view_mode: View mode (e.g., 'teaser', 'full').
 * - teaser: Flag for the teaser state.
 * - page: Flag for the full page state.
 * - readmore: Flag for more state.
 * - logged_in: Flag for authenticated user status.
 * - is_admin: Flag for admin user status.
 * - reading_time: Estimated reading time for articles.
 *
 * @see template_preprocess_node()
 */
#}
{% set classes = [
  'node',
  'node--type-' ~ node.bundle|clean_class,
  node.isPromoted() ? 'node--promoted',
  node.isSticky() ? 'node--sticky',
  not node.isPublished() ? 'node--unpublished',
  view_mode ? 'node--view-mode-' ~ view_mode|clean_class,
] %}
<article{{ attributes.addClass(classes) }} role="article">
  {{ title_prefix }}
  {% if label and not page %}
    <h2{{ title_attributes.addClass('node__title') }}>
      <a href="{{ url }}" rel="bookmark">{{ label }}</a>
    </h2>
  {% endif %}
  {{ title_suffix }}

  {% if display_submitted %}
    <div class="node__meta">
      {% if author_picture %}
        <div class="node__author-picture">
          {{ author_picture }}
        </div>
      {% endif %}
      <span class="node__author" {{ author_attributes }}>
        {{ 'By'|t }} {{ author_name }}
      </span>
      <span class="node__date">
        {{ date }}
      </span>
      {% if reading_time %}
        <span class="node__reading-time">
          {{ reading_time }} {{ 'min read'|t }}
        </span>
      {% endif %}
      {{ metadata }}
    </div>
  {% endif %}

  <div{{ content_attributes.addClass('node__content') }}>
    {{ content }}
  </div>

  {% if readmore %}
    <div class="node__readmore">
      <a href="{{ url }}" class="button button--outlined" rel="bookmark">
        {{ 'Read more'|t }}
        <span class="sr-only">{{ 'about'|t }} {{ label }}</span>
      </a>
    </div>
  {% endif %}
</article>
